# DeepLX to OpenAI API - Deno版本环境变量配置

# 服务器配置
PORT=8000

# DeepLX翻译API配置
TRANSLATION_API_KEY=your-deeplx-api-key
TRANSLATION_API_URL=https://api.deeplx.org/your-key/translate

# 安全配置（可选）
# API密钥保护 - 设置后客户端需要提供此密钥才能访问API
API_KEY_PROTECTION=sk-your-secret-api-key

# CORS源限制 - 限制哪些域名可以访问API（多个用逗号分隔）
# 示例：
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# ALLOWED_ORIGINS=https://*.yourdomain.com,http://localhost:*
# ALLOWED_ORIGINS=*  # 允许所有源（默认）
ALLOWED_ORIGINS=*
