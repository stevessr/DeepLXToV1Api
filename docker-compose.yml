version: '3.8'
services:
  web:
    image: wizerd/deeplx-to-v1-api:latest
    ports:
      - "38888:8000"
    environment:
      - TRANSLATION_API_KEY=your-deeplx-api-key
      # - API_KEY_PROTECTION=your-api-protection-key  # 可选：API密钥保护
      # - ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com  # 可选：CORS源限制
    # volumes:
    #   - ./main.py:/app/main.py
